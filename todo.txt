重构：
	方法写成纯方法，尽量不依赖外部环境，少用闭包，不在方法内改变参数
	方法不超过20行，最多40行，单个文件不超过100行，测试文件也是
	一个单元测试尽量只有一个断言，不多于三个
	类接口，方法option，
	文档
	测试
	$前缀用于类关键字
	
	考虑是否实现typedef
	完成logger
	是否实现yield
	把util方法放到相应对象的wrapper中
	
	不以$开头的对象放到Zero对象下

实现$range

